@"
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Temporarily exclude vendor directory until vendoring issues are resolved
/vendor/

# Exclude archive directory that contains incompatible import paths
/archive/

# Go workspace file
go.work
"@ > .gitignore
